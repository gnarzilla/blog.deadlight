{
  "name": "deadlight",
  "version": 4,
  "description": "Edge-native blog engine platform integrated with multi-protocal proxy and email federation. Full sovereignty",
  "main": "src/index.js",
  "type": "module",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "marked": "^11.2.0",
    "xss": "^1.0.15"
  },
  "scripts": {
    "build": "node scripts/generate-test-user.js > scripts/seed-db.sql",
    "seed-db": "wrangler d1 execute blog_content_new --local --file=./scripts/seed-db.sql",
    "seed-db:remote": "wrangler d1 execute blog_content_new --local --file=./scripts/seed-db.sql",
    "deploy": "npm run build && wrangler deploy",
    "deploy:full": "npm run build && npm run seed-db:remote && wrangler deploy",
    "dev": "npm run build && npm run seed-db && wrangler dev",
    "setup": "npm run build && npm run seed-db && npm run dev",
    "cleanup": "node scripts/cleanup.js",
    "deploy:clean": "npm run deploy && npm run cleanup",
    "reset-db": "rm -f .wrangler/state/v3/d1/miniflare-D1DatabaseObject/*.sqlite* && wrangler d1 execute blog_content_new --local --file=./scripts/seed-db.sql"
  },
  "devDependencies": {
    "wrangler": "^4.51.0"
  }
}
